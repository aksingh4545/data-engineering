# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/data_learning_platform?retryWrites=true&w=majority

# Local Ollama Endpoint (for server-side requests if needed, frontend connects directly to browser's localhost:11434)
OLLAMA_API_URL=http://localhost:11434
